name: text_with_outline
description: "A Flutter package to easily create text with customizable contours and interior styles."
version: 0.0.3
homepage: https://github.com/Alerwann/text_with_outline
repository: https://github.com/Alerwann/text_with_outline

environment:
  sdk: ">=3.0.0 <4.0.0" 
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
